# horo-DCB Reliability Implementation Plan
Approved specification: user's proposed plan in this task, approved with "Implement the proposed plan".
Baseline: a921f828ee6a38fb900555e7782372ebac5955be. Production is on horo-laptop. Source-only isolated checkout on horo-server at /tmp/horo-dcb-reliability-20260905.
Do not install packages or run project tests on horo-PC, horo-server, or production containers. All tests use the existing GitHub Actions ubuntu-latest Docker job. Controller owns commits/pushes and CI orchestration.
Preserve all current features, source history, pinned dependencies, OAuth isolation, model gpt-5.6-luna / medium, data volumes and seven-day rollback. No model/API fallback or prompt retry. No secrets/prompt/image logging. Do not modify main or production until candidate is fully verified.
# Task 1: Baseline and provenance
Publish the 10 existing committed production changes on a dedicated review branch preserving hashes. Obtain full baseline CI (243 test methods). Keep changes isolated and source-matched. Final image must include OCI source/revision labels, recorded immutable identity and complete OCI transfer.
# Task 2: Control panel readiness
Immediately ephemeral defer after admin permission check, then read status with separate 3-second client HTTP timeout and edit original response. Preserve full Components V2 and local modules during sidecar errors. Overview considers enabled/current guild, access state availability, configured channels/mode, authentication and transport availability. Delay status fake proves ack precedes network completion. Tests first.
# Task 3: Request lifecycle and authorization
Implement tightly coupled changes in bot, bridge client, bridge, admin controls and focused tests. Global active AI=2, global waiting=4, one extra waiting per conversation, queue timeout=30s, image download timeout=15s, entire SDK thread start/resume/turn/run timeout=120s, overall Bot accepted work deadline=150s, extra cancellation cleanup<=5s. Existing user cooldown=5s remains. Explicit busy (429) safe error; never replay prompt. Bound admission before attachments. Same-key serialization includes Discord output. Reject waiting jobs when auth generation changes and recheck current roles after waiting (do not reuse stale role snapshot). Register current AI tasks; cleanup counters and locks after success/error/cancellation. Client close cancels tasks before session close.
Bridge handles CancelledError and aiohttp handler_cancellation. Turn interruption bounded5s. SDK initialization bounded30s, account/status RPC bounded2s. General generation timeout interrupts only its turn. TransportClosedError, blocked pre-handle RPC or failed/hung interrupt requires exiting sidecar and Docker restart; auth/quota error must not restart. Graceful cleanup must not leave executor waits keeping container alive. No private SDK patch/fork.
Shared CodexAccess configuration mutation lock serializes admin panels. Configuration generation invalidates accepted/queued stale work. Role change suspends, cancels/waits old jobs, detaches mapping and best-effort archives, persists new roles, resumes. Fail role change without activating new roles; channel removal stays denied even if archive fails. Pure channel addition does not archive chats. Bridge archive coordinates in-flight thread starts/resumes, stopping matching work before snapshot removal; no resurrection race. Preserve channel-per-user and shared Discord Thread mapping semantics.
ThreadStore copy-on-write: persist candidate before in-memory assignment; OSError fails closed and retains original durable mapping. JSON version1 remains, mode0600 atomic replace remains.
CodexAccess explicit legacy_users/roles mode. v1/v2 legacy remains, legacy persistence usesv2, roles usesv3, v3 empty roles alwaysdeny. Corrupt recovery requires channels then roles; only channels cannot reactivate legacy seed. set_roles persistence succeeds before role mode is committed. No new database or schema generation.
GET /v1/status adds active_requests, queued_requests, last_error. New client defaults missing optional fields safely. healthz no account details. /v1/chat request keys unchanged; busy added to safe-code sets. No new cancel endpoint or ports. Authorization with non-ASCII headers must return fixed401.
Safe numeric timing/counters for queue, images, SDK, Discord, result; don't log prompts/images/secrets/raw RPC. Filter successful health probes only; keep health failures/changes. Metrics ephemeral.
# Task 4: Native voice and dependency maintenance
Voice: create_voice_channel(overwrites=...) includes copy of existing CATEGORY policy plus existing owner permission overwrite; preserve effective resulting permissions (not entry policy change). Remove second set_permissions call, keep persistence and failed-cleanup guarantees. Normal creation3calls->2. Add focused behavior tests first.
Dependencies: keep versions. Ignore only independent version bumps of exact-bound openai-codex-cli-bin and pydantic-core using all three version-update semver types, preserving security review. Parent updates reviewed with exact child requirements. CI verifies SDK/runtime versions from requirements.txt rather than duplicated literals. Add OCI source/revision labels to Dockerfile; no extra runtime dependencies. Keep retired 9router PR out of release.
# Task 5: Documentation, final review, candidate and rollout
Update README.md, horo-DCB.md, SECURITY.md, CONTRIBUTING.md as appropriate; source docs distinguish deployment snapshot from defaults. Preserve all old feature compatibility until rollback window done; no cleanup of old volumes or automatic retention changes.
Focused red/green plus full CI Docker clean build, pip check, compileall, isolation, no pip, SDK/CLI check. Test matrix: delayed status; per-key ordering/caps; revocation while queued/downloading/output; archive-vs-start; two admin changes; corrupt access+restart; store write/replace failures; startup/resume/turn/run/interrupt hangs; actual HTTP disconnect; child death/restart/no auth restart; hostile auth/JSON/images; voice permissions and cleanup. CI must not contact Discord/OpenAI/Steam.
Independent review of each task and final whole branch. Review PR preserving previous10commits. Candidate from tested commit; production full OCI transfer identity check, codex healthy before bot start. Real Discord write smoke requires a separately arranged user action; no agent sends Discord test messages without explicit authorization. Keep prior compose/images and necessary state rollback points7days from deployment. No broad volume restore/deletion.
